services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mestrebrototec  # Nova senha simples
      POSTGRES_DB: brototec_cms
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data